import { provideHttpClient } from '@angular/common/http';import { HttpTestingController,provideHttpClientTesting } from '@angular/common/http/testing';import { TestBed } from '@angular/core/testing';import { WorkplaceService } from './workplace.service';
describe('WorkplaceService',()=>{let service:WorkplaceService;let http:HttpTestingController;beforeEach(()=>{TestBed.configureTestingModule({providers:[WorkplaceService,provideHttpClient(),provideHttpClientTesting()]});service=TestBed.inject(WorkplaceService);http=TestBed.inject(HttpTestingController)});afterEach(()=>http.verify());it('loads a floor in one map request',()=>{service.map(7).subscribe(x=>expect(x.floor.id).toBe(7));const req=http.expectOne(r=>r.url.endsWith('/workplaces/floors/7/map'));req.flush({success:true,data:{floor:{id:7},zones:[],desks:[]},timestamp:''})});it('downloads secured plans as blobs',()=>{service.plan(7).subscribe(x=>expect(x.type).toBe('image/png'));const req=http.expectOne(r=>r.url.endsWith('/workplaces/floors/7/plan'));expect(req.request.responseType).toBe('blob');req.flush(new Blob(['x'],{type:'image/png'}))});it('sends one validated batch instead of a request per drag',()=>{service.saveDesks(7,[]).subscribe();const req=http.expectOne(r=>r.url.endsWith('/workplaces/floors/7/desks/batch'));expect(req.request.method).toBe('PUT');expect(req.request.body).toEqual({desks:[]});req.flush({success:true,data:[],timestamp:''})})});
